eureka:
  client:
    register-with-eureka: true  #开启注册功能
    service-url:
      defaultZone: http://localhost:8761/eureka #声明注册地址
spring:
  application:
    name: user  #声明客户端名称

feign:
  hystrix:
    enabled: false #开启服务降级功能